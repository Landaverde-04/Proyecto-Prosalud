from django.db import models

# Sin modelos todavia: HU-25 (definir los modelos del historial medico,
# en pareja con Samuel) no se ha hecho. Las plantillas de este app por
# ahora son solo una vista previa visual con datos de ejemplo en JS
# (ver pacientes/static/pacientes/js/pacientes-mock.js), sin backend real.
